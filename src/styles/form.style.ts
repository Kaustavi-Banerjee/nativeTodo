import { StyleSheet } from "react-native";

const formStyle = StyleSheet.create({
  input: {
    borderWidth: 2,
    borderRadius: 4,
    borderColor: '#827b70',
    padding: 8,
  }
});

export default formStyle;